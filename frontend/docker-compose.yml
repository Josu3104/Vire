version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vire-frontend-test
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
